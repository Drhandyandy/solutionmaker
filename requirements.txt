# PROJECT TOTALITY - Phase 2 Dependencies
# Install with: pip install -r requirements.txt

# Core data processing
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0

# HTTP/ API handling
requests>=2.31.0

# Cryptographic operations
ecdsa>=0.18.0

# Bitcoin-specific libraries (for accurate Z calculation)
python-bitcoinlib>=0.11.0

# Optional: Lattice attack dependencies (for Phase 3)
# fpylll>=0.5.6  # Requires conda/mamba installation
# sageall>=9.0   # Requires SageMath installation

# Note: For fpylll and SageMath, use:
#   pip install condacolab
#   import condacolab
#   condacolab.install()
#   !mamba install -c conda-forge sage fpylll python=3.11
